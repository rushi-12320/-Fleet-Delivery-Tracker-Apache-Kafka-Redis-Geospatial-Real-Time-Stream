# -Fleet-Delivery-Tracker-Apache-Kafka-Redis-Geospatial-Real-Time-Stream
A real-time fleet tracking system that ingests live GPS updates from delivery vehicles through Apache Kafka and uses Redis geospatial indexes to serve instant location queries, geofence alerts, and live map updates at low latency.
